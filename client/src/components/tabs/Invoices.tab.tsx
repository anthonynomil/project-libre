import { Box } from "@mui/material";

export default function InvoicesTab() {
  return (
    <Box sx={{ alignItems: "center", display: "flex", flex: 1, justifyContent: "center" }}>
      Invoices tab is in development...
    </Box>
  );
}
